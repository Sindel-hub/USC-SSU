# Admin Audit Guardrails Static Fix

The **System guardrails / Report guide** card on the Admin Audit & Reports page no longer uses the shared `sticky-card` behavior. It now stays in the normal document flow and scrolls away naturally with the rest of the page.

The shared sticky utility remains available for other admin components that actually benefit from it.
