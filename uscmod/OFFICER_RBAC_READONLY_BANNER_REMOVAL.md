# Officer RBAC Read-only Banner Removal

Removed the large `Read-only module` card from officer modules. Officers can still open unassigned modules in read-only mode, and protected actions remain disabled by RBAC. Compact read-only indicators on navigation/actions are preserved.
