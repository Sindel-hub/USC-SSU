# Student Events Scroll Final Fix

- Scroll down/up shortcuts bind immediately, before Firebase finishes loading.
- Highlight fills the available desktop viewport so Upcoming Events does not peek into the first screen.
- Scroll up stays hidden until Upcoming Events actually enters the viewport.
- Native browser/page scrolling remains free; no snap or automatic section switching is used.
- Only one document-level vertical scrollbar is used on desktop.
